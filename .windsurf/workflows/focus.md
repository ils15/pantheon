---
description: "Pin a session goal to prevent scope creep"
---
# /focus — Session Goal

**What:** Pins a session objective. When active, all agents verify every action against the pinned goal. Off-topic work is flagged and blocked.
**Usage:** `/focus <short objective>`
**Commands:** `/focus list` — show all active goals, `/focus clear` — remove all
