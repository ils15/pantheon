{
  "pantheon": {
    "description": "Gather 3-5 expert perspectives in parallel and synthesize a single recommendation",
    "template": "Run a Pantheon synthesis on this question. Identify the 3-5 most relevant specialist perspectives, dispatch them in parallel with the same question, then synthesize a single authoritative recommendation.\n\nQuestion: {{input}}",
    "agent": "agora"
  },
  "focus": {
    "description": "Pin a session goal — all todos, delegation, and verification stay aligned with it",
    "template": "SESSION GOAL (pinned): {{input}}\n\nAll subsequent tasks, todos, and delegations must stay aligned with this goal. Verify alignment before each major decision. If a task drifts from this goal, flag it and ask for clarification before proceeding.",
    "agent": "zeus"
  },
  "forge": {
    "description": "Configure model provider and plan — lists available plans, applies selection to opencode.json",
    "template": "You are configuring the model provider for this OpenCode installation.\n\nFirst, find the Pantheon installation directory. Check these locations IN ORDER:\n  1. The current workspace root (look for platform/plans/)\n  2. ~/.config/opencode/ (global OpenCode config)\nUse the FIRST location that has a platform/plans/ directory with plan JSON files.\n\nStep 1: List all available plan files in the Pantheon platform/plans/ directory (exclude plan-active.json, schema.json).\nStep 2: Present the user with a numbered list showing plan name, price, and model tiers.\nStep 3: Ask the user which plan to use.\n\nStep 4: Once the user selects a plan, run:\n  bash <pantheon-dir>/platform/select-plan.sh <plan-name>\n\nThis writes models to ~/.config/opencode/opencode.json (NOT the repo).\n\nIf the user provides a specific model ID directly (e.g. /forge anthropic/claude-sonnet-4-20250620), write to ~/.config/opencode/opencode.json:\n  - Set root \"model\" to that ID\n  - Remove all per-agent model overrides\n\nIf the user types \"auto\", write to ~/.config/opencode/opencode.json:\n  - Remove root \"model\" and \"small_model\"\n  - Clear all per-agent model overrides\nso OpenCode uses its defaults.",
    "agent": "zeus"
  },
  "subtask": {
    "description": "Run a bounded child worker and return a structured summary",
    "template": "Run this as a bounded subtask and return a structured summary when complete.\n\nTask: {{input}}\n\nReturn format:\n- Status: DONE | BLOCKED | PARTIAL\n- Summary: 2-3 sentence result\n- Files changed: list\n- Next action: recommendation",
    "subtask": true
  },
  "sketch": {
    "description": "Turn a rough idea into a structured spec through Q&A",
    "template": "You are a requirements analyst. Turn this rough idea into a structured spec through a short Q&A interview.\n\nIdea: {{input}}\n\nAsk 3-5 targeted questions (one at a time) to clarify: scope, constraints, success criteria, non-goals, and tech preferences. After all answers, produce a markdown spec with: Overview, Goals, Non-Goals, Functional Requirements, Technical Constraints, and Open Questions.",
    "agent": "athena"
  },
  "mirrordeps": {
    "description": "Mirror a dependency's source into .deps/ for local inspection",
    "template": "Mirror the source of this dependency into .deps/ (gitignored) for local inspection.\n\nDependency: {{input}}\n\nSteps:\n1. Identify the GitHub repo for the dependency\n2. Run: mkdir -p .deps && git clone --depth=1 <repo-url> .deps/<pkg-name>\n3. Confirm .deps/ is in .gitignore\n4. Report the cloned path and key files to inspect",
    "agent": "apollo"
  },
  "audit": {
    "description": "Ask Themis to audit current changes for quality, coverage and security",
    "template": "Audit the current changes for quality and security.\n\nFocus: {{input}}\n\nSteps:\n1. Run `git diff HEAD` (or `git diff --staged`) to get the change surface\n2. Identify changed files and classify review scope level (Plan/Wave/Task/Final)\n3. Run ruff on changed Python files; Biome on changed JS/TS files\n4. Check test coverage on changed code (target >80%)\n5. Run OWASP Top 10 security checks\n6. Apply self-reflection quality gate (6 categories, 1-10)\n\nReturn: APPROVED / NEEDS_REVISION / FAILED with specific file:line feedback and the scoring table.",
    "agent": "themis"
  },
  "ping": {
    "description": "Ping all 17 Pantheon agents — dispatches to each and reports status",
    "template": "Ping all configured Pantheon agents. For each one, dispatch a task asking them to reply with their name and status. Collect all responses and present a table with: agent_name | role | status (✅ Online / ❌ No response).\n\nIf any agent fails to respond within 10 seconds, mark it as ❌ No response and continue.",
    "agent": "zeus",
    "subtask": true
  },
  "praxis": {
    "description": "Execute plan via task system - creates tasks from Athena plans, dependency ordering, mandatory gates",
    "template": "Execute the plan using the task system.\n\nPlan: {{input}}\n\n1. Create tasks from the plan with dependency ordering\n2. Execute each wave sequentially (parallel within wave, sequential between waves)\n3. Stop at mandatory gates: Plan approval -> Phase review -> Git commit\n4. Route: Zeus -> Hermes/Aphrodite/Demeter -> Themis -> Mnemosyne\n5. Return status summary when complete.",
    "agent": "zeus"
  },
  "metamorphosis": {
    "description": "Intelligent refactoring - LSP analysis, Metis gap analysis, TDD refactoring, Themis verification",
    "template": "Run intelligent refactoring with safety guarantees.\n\nTarget: {{input}}\n\nSteps:\n1. Apollo: LSP analysis of target\n2. Athena + Metis: gap analysis and plan\n3. Hermes: TDD refactoring (RED->GREEN->REFACTOR)\n4. Themis: verification (tests must pass before AND after)\n\nSafety: No behavior changes. Tests must pass before AND after.",
    "agent": "zeus"
  },
  "cancel-relentless": {
    "description": "Cancel relentless mode - stops the self-referential development loop, preserves work",
    "template": "Cancel relentless mode immediately.\n\nStop the self-referential development loop. Preserve all work in progress. Do NOT discard changes, close session, or delete tasks. Return control to the user.",
    "agent": "zeus"
  },
  "stop-continuation": {
    "description": "Stop all auto-continuation - stops relentless mode, auto-continue, and task system continuation",
    "template": "Stop ALL auto-continuation mechanisms immediately.\n\nStop: relentless mode, auto-continue, task system auto-continuation.\nPreserve: manual tools, agent delegation, hooks, safety gates.\n\nReturn control to the user. Do NOT close session or discard work.",
    "agent": "zeus"
  }
}